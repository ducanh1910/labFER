import styles from './News.module.scss';
import classNames from 'classnames/bind';

// eslint-disable-next-line no-unused-vars
const cx = classNames.bind(styles);
function News() {
    return (
        <div>
            <h1>Hãy đón chờ các bộ phim nóng hổi tại đây nhé!!!</h1>
            
        </div>
    );
}

export default News;
